# OpenAI API Key (Required)
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key

# Exchange Rate API Key (Optional, for JPY cost estimation)
# Get your API key from https://exchangerate.host/ (or https://exchangeratesapi.io/ depending on the provider used in server.js)
# Note: The codebase currently uses http://api.exchangerate.host/live
EXCHANGE_RATE_API_KEY=your-exchange-rate-api-key
